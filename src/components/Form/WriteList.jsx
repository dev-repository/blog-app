import React from 'react'

const WriteList = ({ writeList }) => {
    // console.log(writeList);
    return (
        <div>
            <div>
                {/* {writeForm.map((it) => (
                    <div key={it.title}>

                    </div>
                ))} */}
            </div>
        </div>
    )
}

export default WriteList